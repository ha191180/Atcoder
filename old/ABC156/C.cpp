#include <bits/stdc++.h>
using namespace std;

/*
int main(){
  int n; cin >> n;
  vector<int> x(n);
  for (auto &&i : x)
  {
    cin >> i;
  }
  int ans = INT32_MAX;
  for (int i = 1; i <= 100; i++)
  {
    int sum = 0;
    for (auto &&j : x)
    {
      sum += (j - i)*(j - i);
    }
    ans = min(sum, ans);

  }
  
  cout << ans << endl;
}
*/

int main(){
  INT32_C(10);
  cout << INT16_MAX << " " << INT32_MAX  << endl;
  int32_t x = INT32_C(32);
  cout << x;
  x = 2;
  cout << x;
}